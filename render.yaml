services:
  - type: web
    name: aurora-ai
    runtime: python
    plan: free
    buildCommand: pip install -e ".[serve]"
    startCommand: uvicorn aurora.api.__main__:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.10"
